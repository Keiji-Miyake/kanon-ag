/**
 * 二つの数値を加算します。
 * @param a - 加算する最初の数値
 * @param b - 加算する二番目の数値
 * @returns a と b の合計
 */
export const add = (a: number, b: number): number => {
  const result = a + b;
  return result;
};

// 実行例の出力
console.log(add(5, 10));
