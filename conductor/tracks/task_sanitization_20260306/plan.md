# 実装計画: WorktreeManagerの堅牢性のためのタスク名サニタイズ

この計画では、ワークツリーの作成を確実にするために、`WorktreeManager.ts` のタスク名サニタイズを改善する手順を概説します。

---

## フェーズ 1: 調査とテスト設計

- [ ] **タスク: Gitブランチ名の制限と無効な文字の調査**
  *要件:* 標準的な制限（例：255文字だが、ファイルシステム上は100文字が安全）と、避けるべきすべての文字を特定する。

- [ ] **タスク: タスク名サニタイズのための失敗するテストの作成**
  *要件:* `tests/usecases/environment/worktreeManager.test.ts` (または同等のファイル) に新しいテストスイートを作成し、長い名前（100文字以上）、特殊文字、先頭/末尾のスペース、ユニコードのテストケースを含める。
  - [ ] `tests/usecases/environment/worktreeManager.test.ts` に失敗するユニットテストを記述する。
  - [ ] テストを実行し、失敗を確認する（Redフェーズ）。

- [ ] **タスク: Conductor - ユーザー手動検証 '調査とテスト設計' (workflow.mdのプロトコル)**

---

## フェーズ 2: 実装

- [ ] **タスク: `WorktreeManager.ts` への堅牢なサニタイズの実装**
  *要件:* `src/usecases/environment/worktreeManager.ts` の `safeTaskId` ロジックをリファクタリングし、切り詰めとより適切な文字置換を処理できるようにする。
  - [ ] `sanitizeTaskName` メソッド/ユーティリティを実装する。
  - [ ] `setupTaskEnvironment` を更新して新しいサニタイズを使用するようにする。

- [ ] **タスク: 実装とカバレッジの検証**
  *要件:* 新しく作成したテストを実行し、パスすることを確認する。`WorktreeManager.ts` のコードカバレッジが80%以上の要件を満たしていることを確認する。
  - [ ] テストを実行し、パスを確認する（Greenフェーズ）。
  - [ ] カバレッジレポートを実行する（例: `npm run test:unit -- --coverage`）。

- [ ] **タスク: Conductor - ユーザー手動検証 '実装' (workflow.mdのプロトコル)**

---

## フェーズ 3: 完了

- [ ] **タスク: 変更のコミットとドキュメント化**
  *要件:* 実装をコミットし、必要に応じて関連するドキュメントを更新する。
  - [ ] プロジェクトガイドラインに基づいてセルフレビューを行う。
  - [ ] 実装とテストを説明的なメッセージとともにコミットする。

- [ ] **タスク: Conductor - ユーザー手動検証 '完了' (workflow.mdのプロトコル)**
